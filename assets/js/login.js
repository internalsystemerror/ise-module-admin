(function ($)
{
    var constants = {
    },
            properties = {
            },
            methods = {
                init: function ()
                {
                }
            };

    $(document).ready(methods.init);
})(jQuery);